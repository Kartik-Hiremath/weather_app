sonar.projectKey=weather_app
sonar.projectName=Weather App
sonar.projectVersion=1.0

sonar.sources=.
sonar.exclusions=**/*.png,**/*.svg,**/*.jpg,**/*.jpeg

sonar.sourceEncoding=UTF-8

# This is automatically handled by Jenkins; do NOT hardcode the login token here.
# sonar.login=your_sonarqube_token  ← ❌ REMOVE THIS LINE if using Jenkins

